import { SettingsScreen } from "@/components/app/settings";

export const dynamic = "force-dynamic";

export const metadata = {
  title: "Settings · Talise",
};

export default function SettingsPage() {
  return <SettingsScreen />;
}
